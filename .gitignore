# Local Netlify folder
.netlify

# Node deps (restored via npm install)
node_modules

# Local secrets / scratch (never commit)
.keys.local.txt
.env
.env.*

# Clean publish staging dir used for manual CLI deploys (never commit/publish)
.deploy-public/
